# websocket url for your openclaw gateway
# use ssh -L 18789:localhost:18789 user@host if connecting to a remote machine
VITE_GATEWAY_URL=ws://localhost:18789

# gateway auth token — get this from your openclaw instance:
#   openclaw config get gateway.token
VITE_GATEWAY_TOKEN=your-token-here
